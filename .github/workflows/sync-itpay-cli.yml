name: Sync ItPay CLI bundle

on:
  schedule:
    - cron: '23 * * * *'
  workflow_dispatch:

jobs:
  sync:
    uses: itpay-ai/cli/.github/workflows/sync-platform-bundle.yml@main
    permissions:
      contents: write
      pull-requests: write
    with:
      bundle-format: npm-tree
      version-files: package.json
